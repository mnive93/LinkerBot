from django.conf.urls import patterns,include,url
urlpatterns = patterns('main.views',
        url(r'^post/([\w._-]+)$','categorize_links'),
        url(r'^dashboard/$','feed'),
        url(r'^([\w._-]+)/$','profile'),
       
        )
